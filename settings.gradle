pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "EllasGame"

include(":core")
include(":desktop")

// Android sources are scaffolded in ./android.
// Add include(":android") after choosing and configuring the Android Gradle plugin.
